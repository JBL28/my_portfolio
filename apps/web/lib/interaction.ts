/**
 * 포인터 상호작용 공통 상수.
 *
 * 누르기와 끌기를 가르는 거리(px). 이 값을 넘게 움직였으면 "넘기려던 손짓"으로 보고
 * 클릭으로 치지 않는다. 손가락 탭도 완전히 정지하지는 않으므로 0으로 두면 클릭이
 * 아예 안 잡히고, 너무 크게 잡으면 짧은 스와이프가 클릭으로 오인된다. 8px는 브라우저가
 * 탭/드래그를 가르는 기준과 비슷한 값이다.
 *
 * 갤러리 트랙(스와이프 vs 확대)과 오버레이(스와이프 vs 닫기)가 같은 기준을 써야
 * 한 화면 안에서 손짓의 의미가 갈리지 않는다.
 */
export const CLICK_SLOP = 8;
